# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: claude-code-agent
  rootgroup: root
  config: '/etc/claude-code-agent'
  service:
    name: claude-code-agent
  subcomponent:
    config: '/etc/claude-code-agent-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
